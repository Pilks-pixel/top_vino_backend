## Agent skills

### Issue tracker

GitHub Issues in `Pilks-pixel/top_vino_backend`; external pull requests are not a triage surface. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the default `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix` labels. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context documentation. See `docs/agents/domain.md`.
